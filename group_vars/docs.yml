custom_groups:
  - name: docs
    gid: 1787
    state: present

users:
  - name: bill
    groups: sudo, docs
    state: present
  - name: hal
    groups: sudo, docs
    shell: /bin/bash
    state: present

mount_points:
  - { mount_point: "mp0: /mnt/docs,mp=/mnt/docs,backup=0" }
dynamic_container_ids: ["101", "102", "103"] # Adjust as needed
